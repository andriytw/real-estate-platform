# 🔧 Виправлення 404 помилки в Vercel

## Проблема
Vercel показує 404 помилку після deployment.

## Можливі причини:

### 1. Build не завершився успішно
- Перевірте Build Logs в Vercel
- Переконайтеся, що build пройшов без помилок

### 2. Неправильний outputDirectory
- Перевірте, що в `vercel.json` вказано `"outputDirectory": "dist"`
- Перевірте, що Vite створює файли в папці `dist`

### 3. Проблема з routing
- `vercel.json` має правильні `rewrites` для SPA
- Перевірте, що `rewrites` вказує на `/index.html`

## Рішення:

### Крок 1: Перевірте Build Logs
1. Відкрийте deployment в Vercel
2. Перейдіть на вкладку "Logs"
3. Перевірте, чи build пройшов успішно
4. Шукайте помилки типу "Could not resolve" або "Build failed"

### Крок 2: Перевірте налаштування Vercel
1. Settings → General
2. Перевірте:
   - **Build Command:** `npm run build`
   - **Output Directory:** `dist`
   - **Install Command:** `npm install`
   - **Framework Preset:** Vite (або автоматично)

### Крок 3: Перевірте vercel.json
Переконайтеся, що `vercel.json` містить:
```json
{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
```

### Крок 4: Перезапустіть deployment
1. Відкрийте останній deployment
2. Натисніть "Redeploy"
3. Виберіть останній коміт з GitHub
4. Натисніть "Redeploy"

### Крок 5: Перевірте файли в deployment
1. Відкрийте deployment
2. Перейдіть на вкладку "Source"
3. Перевірте, чи є файли:
   - `index.html`
   - `assets/index-*.js`
   - `assets/index-*.css`

## Якщо проблема залишається:

1. **Перевірте Environment Variables:**
   - Чи додані `VITE_SUPABASE_URL` та `VITE_SUPABASE_ANON_KEY`?
   - Чи правильні значення?

2. **Перевірте Build Logs детально:**
   - Шукайте помилки про відсутні файли
   - Перевірте, чи всі залежності встановлені

3. **Спробуйте локальний build:**
   ```bash
   npm run build
   ls -la dist/
   ```
   - Якщо локально працює, а в Vercel ні - проблема в налаштуваннях Vercel

4. **Перевірте версію Node.js:**
   - Vercel може використовувати іншу версію Node.js
   - Додайте `.nvmrc` або вкажіть версію в `package.json`

---

**Останнє оновлення:** 2025-01-XX


