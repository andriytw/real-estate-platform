# 🔑 Інструкція з використання GitHub Token

## Що таке GitHub Token?

GitHub Personal Access Token (PAT) - це токен, який дозволяє автентифікуватися в GitHub без використання пароля. Він потрібен для:
- Push коду на GitHub
- Клонування приватних репозиторіїв
- Автоматизації через API

## Як використовувати токен

### Варіант 1: Використання токену при push (рекомендовано)

Коли ви виконуєте `git push`, GitHub попросить вас ввести:
- **Username**: ваш GitHub username
- **Password**: вставте ваш токен (НЕ пароль!)

```bash
# Приклад команди
git push -u origin main

# Коли попросить:
# Username: ваш_github_username
# Password: ghp_xxxxxxxxxxxxxxxxxxxx (ваш токен)
```

### Варіант 2: Збереження токену в Git Credential Manager

#### Windows (Git Credential Manager):

1. **Перший раз при push:**
   ```bash
   git push -u origin main
   ```
   - Введіть username
   - Введіть токен як password
   - Windows збереже це автоматично

2. **Або вручну через Control Panel:**
   - Відкрийте "Credential Manager" (Диспетчер облікових даних)
   - Windows Credentials → git:https://github.com
   - Додайте/оновіть з вашим токеном

#### Через Git команду:

```bash
# Зберегти токен для GitHub
git config --global credential.helper wincred

# Або для конкретного репозиторію
git config credential.helper store
```

### Варіант 3: Використання токену в URL (небезпечно, не рекомендується)

```bash
# НЕ рекомендується - токен буде видно в історії
git remote set-url origin https://YOUR_TOKEN@github.com/USERNAME/REPO.git
```

## Кроки для push коду

1. **Перевірте, що ви в правильній директорії:**
   ```bash
   cd "G:\My Drive\!Hero rooms\v3 (1)"
   ```

2. **Перевірте статус Git:**
   ```bash
   git status
   ```

3. **Додайте всі файли:**
   ```bash
   git add .
   ```

4. **Створіть commit:**
   ```bash
   git commit -m "Initial commit: Real estate management platform"
   ```

5. **Додайте remote (якщо ще не додано):**
   ```bash
   git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
   ```

6. **Push на GitHub:**
   ```bash
   git push -u origin main
   ```
   
   Коли попросить автентифікацію:
   - **Username**: ваш GitHub username
   - **Password**: вставте ваш токен (починається з `ghp_`)

## Якщо токен не працює

### Перевірте права токену:
Токен повинен мати такі права (scopes):
- ✅ `repo` - повний доступ до репозиторіїв
- ✅ `workflow` - для GitHub Actions (якщо використовуєте)

### Створіть новий токен:
1. Перейдіть на: https://github.com/settings/tokens
2. Натисніть "Generate new token" → "Generate new token (classic)"
3. Назва: "Vercel Deployment" (або будь-яка назва)
4. Виберіть права:
   - ✅ `repo` (повний доступ)
   - ✅ `workflow` (для CI/CD)
5. Натисніть "Generate token"
6. **ВАЖЛИВО**: Скопіюйте токен одразу (він показується тільки один раз!)

## Безпека токену

⚠️ **ВАЖЛИВО:**
- ❌ НЕ діліться токеном з іншими
- ❌ НЕ публікуйте токен у коді
- ❌ НЕ комітьте токен у Git
- ✅ Зберігайте токен в безпечному місці
- ✅ Використовуйте різні токени для різних проектів
- ✅ Видаляйте токени, які більше не використовуються

## Альтернатива: SSH ключі

Якщо не хочете використовувати токени, можете налаштувати SSH:

```bash
# Генерування SSH ключа
ssh-keygen -t ed25519 -C "your_email@example.com"

# Додати ключ до GitHub
# Скопіюйте вміст ~/.ssh/id_ed25519.pub
# Додайте на https://github.com/settings/keys

# Змінити remote на SSH
git remote set-url origin git@github.com:USERNAME/REPO.git
```

## Перевірка налаштування

```bash
# Перевірити remote URL
git remote -v

# Перевірити автентифікацію
git ls-remote origin
```

Якщо команда виконується без помилок - все налаштовано правильно!

