# 🚀 Швидке виправлення логіну

## Проблема
Не можете залогінитися, хоча Environment Variables налаштовані.

## Рішення (2 хвилини)

### Крок 1: Відкрийте Supabase SQL Editor

1. Відкрийте **Supabase Dashboard**
2. Перейдіть до **SQL Editor** (ліва панель)
3. Натисніть **New query**

### Крок 2: Виконайте SQL скрипт

1. Відкрийте файл `supabase/check_and_fix_profile.sql` у вашому проекті
2. Скопіюйте весь SQL код
3. Вставте в SQL Editor
4. Натисніть **Run** (або `Cmd+Enter` / `Ctrl+Enter`)

### Крок 3: Перевірте результат

Після виконання ви побачите:
- ✅ Якщо все добре: `✅ Профіль створено/оновлено для користувача at@herorooms.de`
- ❌ Якщо помилка: Прочитайте повідомлення про помилку

### Крок 4: Спробуйте залогінитися

1. Відкрийте сайт на Vercel
2. Введіть:
   - Email: `at@herorooms.de`
   - Password: `Tsero6730451!`
3. Відкрийте **Console** (F12) і перевірте логи

## Що робить скрипт?

1. **Перевіряє** чи існує користувач `at@herorooms.de` в `auth.users`
2. **Перевіряє** чи є профіль для цього користувача
3. **Створює або оновлює** профіль з правильними даними:
   - `name`: `Super Admin`
   - `department`: `facility`
   - `role`: `super_manager`
   - `is_active`: `true`

## Якщо все ще не працює

1. Перевірте **Console** в браузері - які логи з'являються?
2. Перевірте **RLS Policies**:
   - Table Editor → `profiles` → вкладка **Policies**
   - Має бути 4 policies
3. Якщо policies немає - виконайте `supabase/fix_profiles_rls_no_recursion.sql`

---

**Після виконання скрипту:** Зачекайте 10 секунд і спробуйте залогінитися знову.


