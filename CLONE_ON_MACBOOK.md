# 🍎 Клонування проєкту на MacBook з GitHub

## ✅ Що вже зроблено

- ✅ Всі файли завантажені на GitHub
- ✅ Репозиторій: `https://github.com/andriytw/real-estate-platform.git`
- ✅ `.env.local` НЕ закомічений (захищений в `.gitignore`)

## 🚀 Швидкий старт на MacBook

### Крок 1: Клонуйте репозиторій

Відкрийте Terminal на MacBook та виконайте:

```bash
# Перейдіть до папки, де хочете зберегти проєкт
cd ~/Desktop
# або
cd ~/Documents
# або
cd ~/Projects

# Клонуйте репозиторій
git clone https://github.com/andriytw/real-estate-platform.git

# Перейдіть до папки проєкту
cd real-estate-platform
```

### Крок 2: Встановіть Node.js (якщо ще не встановлено)

```bash
# Перевірте, чи встановлено Node.js:
node --version

# Якщо команда не знайдена, встановіть через Homebrew:
brew install node

# Або завантажте з офіційного сайту:
# https://nodejs.org/ (рекомендовано LTS версію)
```

### Крок 3: Створіть файл .env.local

**ВАЖЛИВО:** Файл `.env.local` містить секретні ключі Supabase і НЕ зберігається в Git.

Створіть файл вручну:

```bash
# Створіть файл .env.local
touch .env.local

# Відкрийте його в редакторі:
open -a TextEdit .env.local
# або
nano .env.local
# або відкрийте в VS Code:
code .env.local
```

Додайте вміст:

```env
NEXT_PUBLIC_SUPABASE_URL=https://qcpuzfhawcondygspiok.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_cpQrhzVqZRCCeULDWhVJJw_ZIhcLx0Y
```

Збережіть файл.

### Крок 4: Встановіть залежності

```bash
# Встановіть всі npm пакети
npm install
```

Це встановить:
- React, Vite
- Supabase клієнти (`@supabase/supabase-js`, `@supabase/ssr`)
- Всі інші залежності

### Крок 5: Запустіть проєкт

```bash
# Запустіть dev сервер
npm run dev
```

Проєкт відкриється на `http://localhost:5173` (або інший порт, якщо 5173 зайнятий).

### Крок 6: Перевірте підключення до Supabase

1. Відкрийте браузер
2. Перейдіть на `/test-db`
3. Має з'явитися повідомлення "Connection Successful" ✅

## 📋 Повна послідовність команд (копіюйте всі разом)

```bash
# 1. Клонуйте репозиторій
cd ~/Desktop
git clone https://github.com/andriytw/real-estate-platform.git
cd real-estate-platform

# 2. Створіть .env.local (скопіюйте вміст вище)
cat > .env.local << 'EOF'
NEXT_PUBLIC_SUPABASE_URL=https://qcpuzfhawcondygspiok.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_cpQrhzVqZRCCeULDWhVJJw_ZIhcLx0Y
EOF

# 3. Встановіть залежності
npm install

# 4. Запустіть проєкт
npm run dev
```

## 🎯 Альтернатива: Використайте скрипт автоматичного налаштування

Якщо ви клонували проєкт, ви можете використати готовий скрипт:

```bash
# Зробіть скрипт виконуваним
chmod +x QUICK_MAC_SETUP.sh

# Запустіть скрипт
./QUICK_MAC_SETUP.sh
```

**Примітка:** Скрипт створить `.env.local` автоматично, але перевірте значення!

## 🔍 Перевірка після клонування

Переконайтеся, що все на місці:

```bash
# Перевірте структуру проєкту
ls -la

# Має бути:
# ✅ package.json
# ✅ vite.config.ts
# ✅ components/
# ✅ utils/
# ✅ .env.local (створіть вручну!)
# ❌ node_modules/ (буде створено після npm install)
```

## 🛠️ Можливі проблеми та рішення

### Проблема 1: "Command not found: git"
**Рішення:** Встановіть Git:
```bash
# Через Homebrew:
brew install git

# Або завантажте з:
# https://git-scm.com/download/mac
```

### Проблема 2: "Command not found: npm"
**Рішення:** Встановіть Node.js (див. Крок 2 вище)

### Проблема 3: Помилки під час `npm install`
**Рішення:**
```bash
# Очистіть кеш
npm cache clean --force

# Видаліть node_modules та package-lock.json
rm -rf node_modules package-lock.json

# Встановіть знову
npm install
```

### Проблема 4: Помилка підключення до Supabase
**Рішення:**
1. Перевірте, що файл `.env.local` існує
2. Перевірте, що ключі правильні
3. Перезапустіть dev сервер після зміни `.env.local`

### Проблема 5: Порт 5173 зайнятий
**Рішення:** Vite автоматично вибере інший порт. Перевірте вивід в терміналі.

## 📝 Важливі примітки

1. **`.env.local` НЕ зберігається в Git** - створіть його вручну на MacBook
2. **`node_modules/` НЕ зберігається в Git** - встановлюється через `npm install`
3. **Всі зміни коду** зберігаються в Git - можете працювати та робити commit/push

## 🔄 Оновлення проєкту (якщо є нові зміни на GitHub)

```bash
# Отримайте останні зміни
git pull origin main

# Якщо додалися нові залежності:
npm install
```

## 🎉 Готово!

Після виконання всіх кроків:
- ✅ Проєкт клоновано з GitHub
- ✅ Залежності встановлено
- ✅ Supabase налаштовано
- ✅ Проєкт працює на MacBook

**Приємної роботи! 🚀**

---

## Посилання

- **GitHub репозиторій:** https://github.com/andriytw/real-estate-platform
- **Node.js:** https://nodejs.org/
- **Git для Mac:** https://git-scm.com/download/mac

