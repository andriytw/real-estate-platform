# Повний промпт проекту: Real Estate Management Platform

## 📋 Огляд проекту

**Назва:** HeroRooms Real Estate Management Platform  
**Тип:** React/Vite SPA для управління нерухомістю  
**Deployment:** Vercel (автоматичний деплой через GitHub)  
**Backend:** Supabase (PostgreSQL + API)  
**GitHub:** https://github.com/andriytw/real-estate-platform

---

## 🛠 Технологічний стек

### Frontend
- **React 19.2.1** - UI бібліотека
- **TypeScript 5.8.2** - типізація
- **Vite 6.2.0** - збірщик та dev сервер
- **Tailwind CSS** - стилізація (через CDN)
- **Lucide React 0.554.0** - іконки
- **Three.js 0.160.0** - 3D візуалізація

### Backend & Database
- **Supabase** - PostgreSQL база даних + REST API
  - `@supabase/supabase-js 2.86.0` - клієнт для браузера
  - `@supabase/ssr 0.8.0` - клієнт для SSR (готово для майбутнього)

### Deployment
- **Vercel** - автоматичний деплой через GitHub
- **GitHub** - версійний контроль

---

## 📁 Структура проекту

```
├── components/              # React компоненти
│   ├── Navbar.tsx          # Навігація
│   ├── FilterBar.tsx       # Фільтри нерухомості
│   ├── PropertyCard.tsx    # Картка нерухомості
│   ├── PropertyDetails.tsx # Деталі нерухомості
│   ├── BookingForm.tsx     # Форма бронювання
│   ├── Marketplace.tsx    # Маркетплейс
│   ├── AccountDashboard.tsx # Дашборд акаунту
│   ├── AdminCalendar.tsx   # Адмін календар
│   ├── SalesCalendar.tsx   # Календар продажів
│   ├── InvoiceModal.tsx    # Модаль інвойсу
│   ├── TestDB.tsx          # Тест підключення Supabase
│   └── ...                 # Інші компоненти
│
├── services/
│   └── supabaseService.ts  # Service слой для роботи з Supabase
│
├── utils/
│   └── supabase/
│       ├── client.ts       # Supabase клієнт для браузера
│       ├── server.ts       # Supabase клієнт для сервера (готово)
│       └── middleware.ts  # Middleware для сесій (готово)
│
├── supabase/
│   ├── schema.sql          # SQL схема БД (основна)
│   ├── schema_safe.sql     # Безпечна версія (тільки політики)
│   └── schema_extended.sql # Розширена версія
│
├── App.tsx                 # Головний компонент
├── types.ts                # TypeScript типи
├── constants.ts            # Константи та mock дані
├── bookingUtils.ts         # Утиліти для бронювань
├── vite.config.ts          # Vite конфігурація
├── vercel.json             # Vercel конфігурація
└── package.json            # Залежності
```

---

## 🗄 Структура бази даних (Supabase)

### Таблиці

1. **properties** - Нерухомість
   - Основні поля: `id`, `title`, `address`, `city`, `district`, `price`, `rooms`, `area`, `status`
   - JSONB поля: `details`, `building`, `inventory`, `meter_readings`, `meter_log`, `tenant`, `rental_history`, `rent_payments`, `owner_expense`, `future_payments`, `repair_requests`, `events`
   - Статуси: `'Available' | 'Reserved' | 'Rented' | 'Maintenance'`

2. **bookings** - Бронювання
   - Основні поля: `id`, `room_id`, `property_id`, `start_date`, `end_date`, `guest`, `status`, `price`, `balance`
   - Статуси: `'reserved' | 'offer_prepared' | 'offer_sent' | 'invoiced' | 'paid' | 'check_in_done' | 'completed'`
   - Типи: `'GUEST' | 'BLOCK'`

3. **offers** - Офери
   - Поля: `id`, `client_name`, `property_id`, `internal_company`, `price`, `dates`, `status`
   - Статуси: `'Draft' | 'Sent' | 'Invoiced'`

4. **invoices** - Інвойси
   - Поля: `id`, `invoice_number`, `date`, `due_date`, `internal_company`, `client_name`, `items` (JSONB), `total_net`, `tax_amount`, `total_gross`, `status`
   - Статуси: `'Paid' | 'Unpaid' | 'Overdue'`

5. **leads** - Ліди
   - Поля: `id`, `name`, `type`, `email`, `phone`, `address`, `status`
   - Типи: `'Company' | 'Private'`
   - Статуси: `'Active' | 'Past' | 'Potential'`

6. **requests** - Запити
   - Поля: `id`, `first_name`, `last_name`, `email`, `phone`, `people_count`, `start_date`, `end_date`, `message`, `property_id`, `status`
   - Статуси: `'pending' | 'processed' | 'archived'`

7. **calendar_events** - Події календаря
   - Поля: `id`, `title`, `property_id`, `booking_id`, `type`, `day`, `date`, `description`, `assignee`, `status`
   - Типи: `'Einzug' | 'Auszug' | 'Putzen' | 'Reklamation' | ...` (багато типів)
   - Статуси: `'open' | 'assigned' | 'done_by_worker' | 'verified' | 'pending' | 'review' | 'archived' | 'completed'`

8. **rooms** - Кімнати/Одиниці
   - Поля: `id`, `name`, `city`, `details`

9. **companies** - Компанії
   - Поля: `id`, `name`, `address`, `iban`, `tax_id`, `logo`, `email`

### Row Level Security (RLS)
- RLS політики створені для всіх таблиць
- Наразі налаштовано для публічного доступу (потрібно налаштувати для продакшн)

### Тригери
- `updated_at` тригери для автоматичного оновлення `updated_at` поля

---

## 🔧 Налаштування

### Environment Variables

**Локально (`.env.local`):**
```env
NEXT_PUBLIC_SUPABASE_URL=https://qcpuzfhawcondygspiok.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_cpQrhzVqZRCCeULDWhVJJw_ZIhcLx0Y
GEMINI_API_KEY=your_gemini_api_key_here
```

**Vercel:**
- Те ж саме через Settings → Environment Variables
- Автоматично застосовується при деплої

### Vite Configuration

`vite.config.ts`:
- Порт: `3000`
- Host: `0.0.0.0`
- Підтримка обох префіксів: `VITE_` та `NEXT_PUBLIC_`
- Code splitting для React та іконок

---

## 📊 TypeScript Типи

Основні інтерфейси в `types.ts`:

- **Property** - нерухомість з усіма деталями
- **Booking** - бронювання зі статусами
- **OfferData** - офери
- **InvoiceData** - інвойси
- **Lead** - ліди
- **RequestData** - запити
- **CalendarEvent** - події календаря
- **Room** - кімнати
- **CompanyDetails** - компанії
- **FilterState** - стан фільтрів

---

## 🔄 Поточний стан інтеграції

### ✅ Реалізовано

1. **Supabase підключення:**
   - Клієнт налаштовано (`utils/supabase/client.ts`)
   - Service слой створено (`services/supabaseService.ts`)
   - SQL схема готова (`supabase/schema.sql`)

2. **Properties (Нерухомість):**
   - ✅ Завантаження з Supabase
   - ✅ Fallback на mock дані якщо БД порожня
   - ✅ Обробка помилок
   - ✅ Трансформація даних (DB ↔ App формат)

3. **Error Handling:**
   - ✅ Обробка помилок "Unregistered API key"
   - ✅ Fallback на mock дані
   - ✅ Помилки не показуються як критичні якщо це попередження

4. **Security:**
   - ✅ React оновлено до 19.2.1 (безпечна версія)
   - ✅ @supabase/ssr оновлено до 0.8.0
   - ✅ Всі вразливості виправлені

5. **Deployment:**
   - ✅ GitHub налаштовано
   - ✅ Vercel підключено
   - ✅ Автоматичний деплой працює

### ⏳ Частково реалізовано / Потрібно доробити

1. **Bookings (Бронювання):**
   - ⚠️ Service методи створені, але не інтегровані в UI
   - ⚠️ Потрібно підключити збереження/завантаження в `BookingForm.tsx`

2. **Offers (Офери):**
   - ⚠️ Service методи створені, але не інтегровані в UI

3. **Invoices (Інвойси):**
   - ⚠️ Service методи створені, але не інтегровані в UI

4. **Leads (Ліди):**
   - ⚠️ Service методи створені, але не інтегровані в UI

5. **Requests (Запити):**
   - ⚠️ Service методи створені, але не інтегровані в UI

6. **Calendar Events:**
   - ⚠️ Service методи створені, але не інтегровані в UI

7. **Row Level Security (RLS):**
   - ⚠️ Політики створені, але налаштовані для публічного доступу
   - ⚠️ Потрібно налаштувати для продакшн (автентифікація)

---

## 🎯 Що потрібно зробити

### Пріоритет 1: Інтеграція UI з Supabase

1. **Bookings:**
   - Підключити `bookingsService` в `BookingForm.tsx`
   - Замінити локальне збереження на Supabase
   - Завантажувати бронювання з БД при відкритті календаря

2. **Offers:**
   - Підключити `offersService` в компонентах оферів
   - Зберігати офери в БД

3. **Invoices:**
   - Підключити `invoicesService` в `InvoiceModal.tsx`
   - Зберігати інвойси в БД

4. **Calendar Events:**
   - Підключити `calendarEventsService` в `AdminCalendar.tsx` та `SalesCalendar.tsx`
   - Синхронізувати події з БД

### Пріоритет 2: Безпека

1. **Row Level Security:**
   - Налаштувати RLS політики для продакшн
   - Додати автентифікацію (Supabase Auth)
   - Обмежити доступ до даних

2. **Environment Variables:**
   - Переконатися що всі ключі безпечні
   - Використовувати service_role ключ тільки на сервері

### Пріоритет 3: Додаткові функції

1. **Real-time updates:**
   - Підключити Supabase Realtime для оновлень
   - Оновлювати UI автоматично при змінах в БД

2. **File Storage:**
   - Використовувати Supabase Storage для зображень
   - Завантажувати зображення нерухомості

3. **Search & Filters:**
   - Покращити фільтри з підтримкою Supabase queries
   - Додати пошук по тексту

---

## 📝 Важливі деталі реалізації

### Service Layer Pattern

Всі операції з БД виконуються через `services/supabaseService.ts`:

```typescript
// Приклад використання
import { propertiesService } from './services/supabaseService';

// Отримати всі
const properties = await propertiesService.getAll();

// Отримати по ID
const property = await propertiesService.getById(id);

// Створити
const newProperty = await propertiesService.create(propertyData);

// Оновити
const updated = await propertiesService.update(id, updates);

// Видалити
await propertiesService.delete(id);
```

### Data Transformation

Service слой автоматично трансформує дані між форматом БД та форматом додатку:

- **DB → App:** `transformPropertyFromDB()`, `transformBookingFromDB()`, etc.
- **App → DB:** `transformPropertyToDB()`, `transformBookingToDB()`, etc.

### Error Handling

- Помилки обробляються в service шарі
- Fallback на mock дані якщо БД недоступна
- Помилки не показуються як критичні якщо це попередження

---

## 🚀 Команди для роботи

```bash
# Розробка
npm run dev              # Запуск на localhost:3000

# Білд
npm run build            # Збірка для продакшн
npm run preview          # Перегляд продакшн збірки

# Git
git add .
git commit -m "message"
git push origin main     # Автоматичний деплой на Vercel
```

---

## 🔗 Посилання

- **GitHub:** https://github.com/andriytw/real-estate-platform
- **Vercel:** Автоматичний деплой через GitHub
- **Supabase:** https://qcpuzfhawcondygspiok.supabase.co

---

## 📌 Примітки

1. **Mock дані:** Використовуються як fallback якщо БД порожня або недоступна
2. **Environment Variables:** Потрібні для роботи з Supabase
3. **SQL Schema:** Потрібно виконати в Supabase SQL Editor перед використанням
4. **RLS:** Наразі налаштовано для публічного доступу (потрібно змінити для продакшн)
5. **Deployment:** Автоматичний через Vercel при push до GitHub

---

## 🎯 Наступні кроки

1. Інтегрувати bookings в UI
2. Інтегрувати offers в UI
3. Інтегрувати invoices в UI
4. Інтегрувати calendar events в UI
5. Налаштувати RLS для продакшн
6. Додати автентифікацію
7. Підключити Realtime updates
8. Додати file storage для зображень

---

**Останнє оновлення:** Після виправлення вразливостей React та @supabase/ssr  
**Статус:** Базова інтеграція Supabase завершена, потрібна інтеграція UI компонентів

