# 🚀 Інструкція з деплою

## Крок 1: Підготовка до GitHub

1. **Ініціалізуйте Git репозиторій (якщо ще не зроблено):**
   ```bash
   git init
   git add .
   git commit -m "Initial commit: Real estate management platform"
   ```

2. **Створіть репозиторій на GitHub:**
   - Перейдіть на https://github.com/new
   - Створіть новий репозиторій (наприклад, `real-estate-platform`)
   - НЕ додавайте README, .gitignore або license (вони вже є)

3. **Підключіть локальний репозиторій до GitHub:**
   ```bash
   git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
   git branch -M main
   git push -u origin main
   ```

## Крок 2: Деплой на Vercel

### Варіант A: Через веб-інтерфейс (рекомендовано)

1. **Зареєструйтесь/увійдіть на Vercel:**
   - Перейдіть на https://vercel.com
   - Увійдіть через GitHub

2. **Створіть новий проект:**
   - Натисніть "Add New..." → "Project"
   - Імпортуйте ваш GitHub репозиторій
   - Vercel автоматично визначить налаштування:
     - Framework Preset: Vite
     - Build Command: `npm run build`
     - Output Directory: `dist`
     - Install Command: `npm install`

3. **Додайте змінні оточення (опціонально):**
   - Якщо використовуєте Gemini API, додайте:
     - Name: `GEMINI_API_KEY`
     - Value: ваш API ключ
   - Натисніть "Add" для кожної змінної

4. **Деплой:**
   - Натисніть "Deploy"
   - Зачекайте завершення збірки (зазвичай 1-2 хвилини)
   - Ваш сайт буде доступний за адресою: `https://your-project-name.vercel.app`

### Варіант B: Через Vercel CLI

1. **Встановіть Vercel CLI:**
   ```bash
   npm install -g vercel
   ```

2. **Увійдіть:**
   ```bash
   vercel login
   ```

3. **Деплой:**
   ```bash
   vercel
   ```
   - Відповідайте на питання (за замовчуванням все правильно)
   - Для продакшн: `vercel --prod`

## Крок 3: Налаштування домену (опціонально)

1. В Vercel Dashboard → Settings → Domains
2. Додайте ваш домен
3. Дотримуйтесь інструкцій для налаштування DNS

## Автоматичний деплой

Після налаштування, кожен push до `main` гілки автоматично створює новий деплой:
- Preview деплої для інших гілок
- Production деплої для `main` гілки

## Перевірка деплою

Після успішного деплою перевірте:
- ✅ Сайт відкривається без помилок
- ✅ Всі роути працюють (SPA routing)
- ✅ Статичні ресурси завантажуються
- ✅ API ключі працюють (якщо використовуються)

## Вирішення проблем

### Помилка збірки
- Перевірте логи в Vercel Dashboard
- Переконайтесь, що всі залежності в `package.json`
- Перевірте, що Node.js версія підтримується (18+)

### Помилки змінних оточення
- Перевірте, що змінні додані в Vercel Dashboard
- Переконайтесь, що назви змінних точно співпадають

### Проблеми з роутингом
- `vercel.json` вже налаштований для SPA
- Переконайтесь, що rewrites працюють

## Підтримка

Якщо виникли проблеми:
1. Перевірте логи в Vercel Dashboard
2. Перевірте локальну збірку: `npm run build`
3. Перевірте preview: `npm run preview`

