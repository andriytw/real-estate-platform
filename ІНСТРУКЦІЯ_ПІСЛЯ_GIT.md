# 📋 Інструкція після встановлення Git

## Крок 1: Перевірте, що Git працює

Відкрийте Terminal в Cursor (`Ctrl + ~`) і виконайте:
```powershell
git --version
```

Якщо бачите версію (наприклад, `git version 2.x.x`) - Git працює! ✅

---

## Крок 2: Виконайте команди

### Варіант A: Використати готовий скрипт

1. Відкрийте файл `GIT_КОМАНДИ.ps1` в Cursor
2. Замініть `Ваше Ім'я` та `your.email@example.com` на ваші дані
3. Відкрийте Terminal (`Ctrl + ~`)
4. Виконайте:
```powershell
.\GIT_КОМАНДИ.ps1
```

### Варіант B: Виконати команди вручну

Відкрийте Terminal і виконайте по черзі:

```powershell
# 1. Перейдіть в директорію проекту
cd "G:\My Drive\!Hero rooms\v3 (1)"

# 2. Ініціалізація Git
git init

# 3. Налаштування (замініть на ваші дані)
git config user.name "Ваше Ім'я"
git config user.email "your.email@example.com"

# 4. Додавання файлів
git add .

# 5. Створення commit
git commit -m "Initial commit: Real estate management platform"

# 6. Встановлення гілки main
git branch -M main
```

---

## Крок 3: Створіть репозиторій на GitHub

1. Відкрийте браузер
2. Перейдіть на: https://github.com/new
3. Введіть назву репозиторію (наприклад: `real-estate-platform`)
4. **ВАЖЛИВО:** НЕ додавайте README, .gitignore або license (вони вже є в проекті)
5. Натисніть "Create repository"

---

## Крок 4: Підключіть до GitHub

Після створення репозиторію, GitHub покаже вам команди. Виконайте:

```powershell
# Замініть YOUR_USERNAME та YOUR_REPO_NAME на ваші дані
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
```

---

## Крок 5: Завантажте код на GitHub

```powershell
git push -u origin main
```

**Коли Git попросить автентифікацію:**
- **Username:** ваш GitHub username
- **Password:** вставте ваш GitHub токен (НЕ пароль!)
- Токен можна знайти або створити на: https://github.com/settings/tokens

Windows автоматично збереже токен для майбутніх операцій.

---

## ✅ Готово!

Після успішного `git push` ваш код буде на GitHub!

Далі можна:
1. Перейти на Vercel
2. Підключити GitHub репозиторій
3. Автоматично задеплоїти проект

---

## ❓ Проблеми?

Якщо щось не працює:
- Перевірте, що Git працює: `git --version`
- Перевірте, що ви в правильній директорії
- Переконайтесь, що репозиторій створений на GitHub
- Перевірте правильність URL репозиторію

Покажіть мені помилку, і я допоможу вирішити!

