# 🔧 Відновлення зв'язку з GitHub

## ✅ Поточний стан

**Remote налаштовано:**
- URL: `https://github.com/andriytw/real-estate-platform.git`
- Статус: ✅ Працює через термінал

**Останній push:** Успішно виконано (коміт b44ef19)

---

## 🔐 Варіанти автентифікації

### Варіант 1: Personal Access Token (рекомендовано)

1. **Створіть токен:**
   - Перейдіть: https://github.com/settings/tokens
   - Натисніть **"Generate new token"** → **"Generate new token (classic)"**
   - Назва: `real-estate-platform`
   - Права: оберіть `repo` (повний доступ до репозиторіїв)
   - Натисніть **"Generate token"**
   - **ВАЖЛИВО:** Скопіюйте токен одразу (він більше не показується!)

2. **Використання токену:**

   **Через термінал:**
   ```bash
   git remote set-url origin https://YOUR_TOKEN@github.com/andriytw/real-estate-platform.git
   ```
   
   АБО використовуйте токен як пароль при push:
   ```bash
   git push origin main
   # Username: andriytw
   # Password: YOUR_TOKEN (не пароль, а токен!)
   ```

   **Через GitHub Desktop:**
   - Settings → Accounts → Sign out
   - Sign in → виберіть "Sign in with a token"
   - Вставте токен

### Варіант 2: SSH ключ (для постійного використання)

1. **Перевірте, чи є SSH ключ:**
   ```bash
   ls -la ~/.ssh/id_rsa.pub
   ```

2. **Якщо немає - створіть:**
   ```bash
   ssh-keygen -t ed25519 -C "your_email@example.com"
   # Натисніть Enter для всіх питань (або введіть пароль)
   ```

3. **Скопіюйте публічний ключ:**
   ```bash
   cat ~/.ssh/id_rsa.pub
   # Скопіюйте весь вміст
   ```

4. **Додайте ключ на GitHub:**
   - Перейдіть: https://github.com/settings/keys
   - Натисніть **"New SSH key"**
   - Title: `MacBook` (або назва комп'ютера)
   - Key: вставте скопійований ключ
   - Натисніть **"Add SSH key"**

5. **Змініть remote на SSH:**
   ```bash
   git remote set-url origin git@github.com:andriytw/real-estate-platform.git
   ```

6. **Перевірте підключення:**
   ```bash
   ssh -T git@github.com
   # Має показати: "Hi andriytw! You've successfully authenticated..."
   ```

### Варіант 3: GitHub CLI (gh)

1. **Встановіть GitHub CLI:**
   ```bash
   brew install gh
   ```

2. **Авторизуйтесь:**
   ```bash
   gh auth login
   # Виберіть GitHub.com → HTTPS → Authenticate Git with your GitHub credentials
   ```

3. **Перевірте:**
   ```bash
   gh auth status
   ```

---

## 🔄 Оновлення remote URL

Якщо потрібно змінити URL:

**HTTPS:**
```bash
git remote set-url origin https://github.com/andriytw/real-estate-platform.git
```

**SSH:**
```bash
git remote set-url origin git@github.com:andriytw/real-estate-platform.git
```

**З токеном:**
```bash
git remote set-url origin https://YOUR_TOKEN@github.com/andriytw/real-estate-platform.git
```

---

## 🧪 Тестування підключення

**Перевірка remote:**
```bash
git remote -v
```

**Тест підключення:**
```bash
git fetch origin
```

**Тест push:**
```bash
git push origin main
```

---

## 🐛 Вирішення проблем

### Проблема: "Authentication failed"

**Рішення:**
1. Перевірте, чи правильний токен/пароль
2. Для HTTPS: використовуйте Personal Access Token замість пароля
3. Для SSH: перевірте, чи ключ додано на GitHub

### Проблема: "Permission denied"

**Рішення:**
1. Перевірте, чи ви маєте доступ до репозиторію
2. Перевірте, чи репозиторій не архівований
3. Перевірте права токену (має бути `repo`)

### Проблема: GitHub Desktop не підключається

**Рішення:**
1. GitHub Desktop → Settings → Accounts → Sign out
2. Sign in знову
3. Або використовуйте термінал для push

---

## ✅ Рекомендація

**Для MacBook найкраще:**
1. Створіть SSH ключ (один раз)
2. Додайте на GitHub
3. Змініть remote на SSH
4. Після цього push/pull працюватимуть автоматично

**Або:**
- Використовуйте Personal Access Token
- Зберігайте його в Keychain (macOS зробить це автоматично)

---

## 📝 Швидка інструкція

**Якщо хочете швидко відновити:**

1. Створіть токен: https://github.com/settings/tokens
2. Виконайте в терміналі:
   ```bash
   git push origin main
   # Username: andriytw
   # Password: [вставте токен]
   ```
3. macOS збереже токен в Keychain

**Або налаштуйте SSH (один раз, потім працює завжди):**

1. Створіть SSH ключ (якщо немає)
2. Додайте на GitHub
3. Змініть remote:
   ```bash
   git remote set-url origin git@github.com:andriytw/real-estate-platform.git
   ```

---

**Останнє оновлення:** 2 січня 2026
