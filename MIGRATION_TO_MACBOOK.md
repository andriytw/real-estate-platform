# Міграція проєкту на MacBook

> **ОНОВЛЕНО:** Проєкт вже завантажено на GitHub! 
> Використайте інструкцію `CLONE_ON_MACBOOK.md` для швидкого клонування.

## Крок 1: Підготовка файлів на Windows

### Що потрібно скопіювати:

1. **Всі файли проєкту** (крім `node_modules` - він зайвий):
   - Скопіюйте всю папку проєкту на MacBook
   - Або використайте Git для клонування (якщо проєкт вже на GitHub)

2. **Важливі файли, які ОБОВ'ЯЗКОВО потрібні:**
   ```
   ✅ package.json
   ✅ package-lock.json (якщо є)
   ✅ tsconfig.json
   ✅ vite.config.ts
   ✅ .env.local (ВАЖЛИВО! Містить Supabase ключі)
   ✅ .gitignore
   ✅ Всі файли .tsx, .ts, .css
   ✅ Всі папки: components/, utils/, тощо
   ```

3. **Що НЕ потрібно копіювати:**
   ```
   ❌ node_modules/ (буде встановлено заново)
   ❌ dist/ (буде згенеровано)
   ❌ .DS_Store (Mac створить сам)
   ```

## Крок 2: Перенесення на MacBook

### Варіант A: Через USB/Зовнішній диск
1. Підключіть USB-накопичувач або зовнішній диск
2. Скопіюйте всю папку проєкту (без `node_modules`)
3. Перенесіть на MacBook

### Варіант B: Через Git (РЕКОМЕНДОВАНО)
Якщо проєкт вже на GitHub:
```bash
# На MacBook:
git clone <ваш-github-url>
cd v3
```

### Варіант C: Через Google Drive/OneDrive
1. Завантажте файли через веб-інтерфейс
2. Завантажте на MacBook

## Крок 3: Налаштування на MacBook

### 1. Встановіть Node.js та npm

```bash
# Перевірте, чи встановлено Node.js:
node --version
npm --version

# Якщо ні - встановіть через Homebrew:
brew install node

# Або завантажте з офіційного сайту:
# https://nodejs.org/
```

### 2. Встановіть залежності проєкту

```bash
# Перейдіть до папки проєкту:
cd ~/Desktop/v3  # або ваш шлях

# Встановіть пакети:
npm install
```

Це встановить всі залежності, включаючи:
- `@supabase/supabase-js`
- `@supabase/ssr`
- React, Vite, та інші

### 3. Створіть файл .env.local

**ВАЖЛИВО:** Файл `.env.local` містить секретні ключі Supabase. Переконайтеся, що він скопійований!

Якщо файл не скопіювався, створіть його вручну:

```bash
# Створіть файл .env.local в корені проєкту:
touch .env.local
```

Додайте вміст (з вашого Windows проєкту):
```env
NEXT_PUBLIC_SUPABASE_URL=https://qcpuzfhawcondygspiok.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_cpQrhzVqZRCCeULDWhVJJw_ZIhcLx0Y
```

### 4. Перевірте налаштування Vite

Файл `vite.config.ts` має правильно налаштувати змінні середовища. Переконайтеся, що він містить:

```typescript
envPrefix: ['VITE_', 'NEXT_PUBLIC_']
```

## Крок 4: Запуск проєкту

```bash
# Запустіть dev сервер:
npm run dev
```

Проєкт має відкритися на `http://localhost:5173` (або інший порт, якщо 5173 зайнятий).

## Крок 5: Перевірка підключення до Supabase

1. Відкрийте браузер
2. Перейдіть на `/test-db` (або на головну сторінку, якщо тестова сторінка встановлена як початкова)
3. Перевірте, чи з'являється повідомлення "Connection Successful"

## Крок 6: Налаштування Git (якщо потрібно)

```bash
# Перевірте, чи налаштовано Git:
git remote -v

# Якщо потрібно додати remote:
git remote add origin <ваш-github-url>

# Перевірте статус:
git status
```

## Можливі проблеми та рішення

### Проблема 1: "Command not found: npm"
**Рішення:** Встановіть Node.js:
```bash
brew install node
```

### Проблема 2: Помилки під час `npm install`
**Рішення:** 
```bash
# Очистіть кеш:
npm cache clean --force

# Видаліть node_modules та package-lock.json:
rm -rf node_modules package-lock.json

# Встановіть знову:
npm install
```

### Проблема 3: Помилки з правами доступу
**Рішення:**
```bash
# Надайте права на виконання:
chmod +x node_modules/.bin/*
```

### Проблема 4: Помилки з шляхами (Windows vs Mac)
**Рішення:** Mac використовує `/` замість `\`. Переконайтеся, що всі імпорти використовують відносні шляхи:
```typescript
// ✅ Правильно:
import { createClient } from '../utils/supabase/client'

// ❌ Неправильно (Windows-специфічні шляхи):
import { createClient } from '..\\utils\\supabase\\client'
```

### Проблема 5: .env.local не працює
**Рішення:**
1. Переконайтеся, що файл знаходиться в корені проєкту
2. Перевірте, що змінні мають префікс `NEXT_PUBLIC_` або `VITE_`
3. Перезапустіть dev сервер після зміни `.env.local`

## Переваги роботи на MacBook

✅ **Немає проблем з Google Drive** - файли на локальному диску
✅ **Швидша робота npm** - немає блокувань синхронізації
✅ **Краща підтримка Node.js** - Mac краще працює з Node.js екосистемою
✅ **Unix-подібна система** - стандарт для веб-розробки

## Наступні кроки після міграції

1. ✅ Перевірте, що проєкт запускається: `npm run dev`
2. ✅ Перевірте підключення до Supabase: `/test-db`
3. ✅ Перевірте всі функції проєкту
4. ✅ Зробіть commit змін (якщо використовуєте Git)
5. ✅ Задеплойте на Vercel (якщо потрібно)

## Додаткові поради

- **Використовуйте VS Code** на MacBook - він працює однаково на всіх платформах
- **Налаштуйте Git** з вашими даними:
  ```bash
  git config --global user.name "Ваше Ім'я"
  git config --global user.email "ваш@email.com"
  ```
- **Встановіть корисні розширення VS Code:**
  - ESLint
  - Prettier
  - GitLens
  - Vite (якщо є)

## Контакти та підтримка

Якщо виникнуть проблеми під час міграції, перевірте:
1. Версію Node.js: `node --version` (рекомендовано: v18+ або v20+)
2. Версію npm: `npm --version` (рекомендовано: 9+)
3. Чи правильно скопійовано файл `.env.local`
4. Чи всі залежності встановлені: `npm list`

---

**Успішної міграції! 🚀**

