# 🔧 Налаштування Git для Windows

## Варіант 1: Встановити Git для Windows (рекомендовано)

### Крок 1: Завантажити Git
1. Перейдіть на: https://git-scm.com/download/win
2. Завантажте останню версію Git for Windows
3. Запустіть установку

### Крок 2: Налаштування під час установки
- Використовуйте налаштування за замовчуванням
- Оберіть "Git from the command line and also from 3rd-party software"
- Оберіть "Use bundled OpenSSH"
- Оберіть "Use the OpenSSL library"
- Оберіть "Checkout Windows-style, commit Unix-style line endings"
- Оберіть "Use MinTTY"
- Оберіть "Enable file system caching"

### Крок 3: Після установки
1. Перезапустіть PowerShell або Command Prompt
2. Перевірте установку:
   ```powershell
   git --version
   ```

### Крок 4: Налаштуйте Git (перший раз)
```powershell
git config --global user.name "Ваше Ім'я"
git config --global user.email "your.email@example.com"
```

## Варіант 2: GitHub Desktop (простіше для початківців)

### Переваги:
- ✅ Графічний інтерфейс
- ✅ Не потрібно знати команди
- ✅ Автоматично налаштовує Git
- ✅ Легко працювати з токенами

### Встановлення:
1. Перейдіть на: https://desktop.github.com/
2. Завантажте та встановіть GitHub Desktop
3. Увійдіть через GitHub
4. Створіть новий репозиторій або клонуйте існуючий

### Як використовувати:
1. Відкрийте GitHub Desktop
2. File → Add Local Repository
3. Виберіть папку: `G:\My Drive\!Hero rooms\v3 (1)`
4. Натисніть "Publish repository" для завантаження на GitHub
5. GitHub Desktop автоматично використає ваш токен

## Варіант 3: Visual Studio Code з Git

Якщо ви використовуєте VS Code:

1. Встановіть розширення "GitLens" (опціонально)
2. VS Code має вбудовану підтримку Git
3. Відкрийте панель Source Control (Ctrl+Shift+G)
4. Натисніть "Initialize Repository"
5. Зробіть commit та push через інтерфейс VS Code

## Після встановлення Git

### Ініціалізація репозиторію:
```powershell
cd "G:\My Drive\!Hero rooms\v3 (1)"
git init
git add .
git commit -m "Initial commit: Real estate management platform"
```

### Підключення до GitHub:
```powershell
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
git branch -M main
```

### Push з токеном:
```powershell
git push -u origin main
# Username: ваш_github_username
# Password: ваш_токен (ghp_...)
```

## Перевірка налаштування

Після встановлення Git, перевірте:
```powershell
git --version
git config --global --list
```

## Допомога

Якщо виникли проблеми:
- Перезапустіть PowerShell після встановлення Git
- Перевірте, що Git в PATH: `$env:PATH`
- Спробуйте використати GitHub Desktop для простішого старту

