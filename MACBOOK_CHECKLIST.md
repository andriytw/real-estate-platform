# ✅ Чеклист міграції на MacBook

## Перед перенесенням (на Windows)

- [ ] Переконайтеся, що всі зміни збережені
- [ ] Зробіть commit у Git (якщо використовуєте)
- [ ] Запишіть значення з `.env.local` (Supabase ключі)
- [ ] Перевірте, що проєкт працює локально

## Перенесення файлів

- [ ] Скопіюйте всю папку проєкту на MacBook
- [ ] **НЕ копіюйте** `node_modules/` (буде встановлено заново)
- [ ] Переконайтеся, що `.env.local` скопійовано (або запишіть значення)

## Налаштування на MacBook

### 1. Встановлення Node.js
- [ ] Перевірте: `node --version` (має бути v18+ або v20+)
- [ ] Якщо немає - встановіть: `brew install node` або з https://nodejs.org/

### 2. Встановлення залежностей
- [ ] Відкрийте Terminal на MacBook
- [ ] Перейдіть до папки проєкту: `cd ~/path/to/project`
- [ ] Встановіть пакети: `npm install`
- [ ] Перевірте, що немає помилок

### 3. Налаштування .env.local
- [ ] Перевірте, що файл `.env.local` існує в корені проєкту
- [ ] Якщо немає - створіть з вашими Supabase ключами:
  ```
  NEXT_PUBLIC_SUPABASE_URL=https://qcpuzfhawcondygspiok.supabase.co
  NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_cpQrhzVqZRCCeULDWhVJJw_ZIhcLx0Y
  ```

### 4. Запуск проєкту
- [ ] Запустіть: `npm run dev`
- [ ] Відкрийте браузер: `http://localhost:5173`
- [ ] Перевірте, що проєкт відкрився

### 5. Перевірка Supabase
- [ ] Перейдіть на `/test-db`
- [ ] Перевірте, що з'являється "Connection Successful"
- [ ] Якщо помилка - перевірте `.env.local`

## Перевірка функціональності

- [ ] Всі компоненти відображаються
- [ ] Навігація працює
- [ ] Форми працюють
- [ ] Дані зберігаються (якщо є localStorage)
- [ ] Supabase підключення працює

## Git налаштування (опціонально)

- [ ] Налаштуйте Git:
  ```bash
  git config --global user.name "Ваше Ім'я"
  git config --global user.email "ваш@email.com"
  ```
- [ ] Перевірте remote: `git remote -v`
- [ ] Якщо потрібно - додайте: `git remote add origin <url>`

## Готово! 🎉

- [ ] Проєкт працює на MacBook
- [ ] Всі функції працюють
- [ ] Supabase підключено
- [ ] Готово до роботи!

---

## Швидкий старт (якщо все готово)

```bash
# 1. Перейдіть до проєкту
cd ~/path/to/project

# 2. Встановіть залежності (якщо ще не встановлено)
npm install

# 3. Запустіть проєкт
npm run dev

# 4. Відкрийте браузер
open http://localhost:5173
```

## Якщо щось не працює

1. Перевірте версію Node.js: `node --version`
2. Очистіть кеш: `npm cache clean --force`
3. Видаліть node_modules: `rm -rf node_modules package-lock.json`
4. Встановіть знову: `npm install`
5. Перевірте `.env.local`
6. Перезапустіть dev сервер

---

**Детальні інструкції:** Дивіться `MIGRATION_TO_MACBOOK.md`

