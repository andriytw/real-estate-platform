# 🚀 Деплой на Vercel - Інструкції

## ✅ Налаштування Vercel

### 1. Підключення GitHub репозиторію

1. **Перейдіть на Vercel:**
   - Відкрийте: https://vercel.com
   - Увійдіть через GitHub

2. **Створіть новий проект:**
   - Натисніть "Add New..." → "Project"
   - Виберіть репозиторій: `andriytw/real-estate-platform`
   - Натисніть "Import"

3. **Налаштування проекту:**
   - **Framework Preset:** Vite (автоматично визначиться)
   - **Root Directory:** `./` (залиште як є)
   - **Build Command:** `npm run build` (вже в `vercel.json`)
   - **Output Directory:** `dist` (вже в `vercel.json`)
   - **Install Command:** `npm install` (вже в `vercel.json`)

### 2. Environment Variables (Змінні оточення)

**ВАЖЛИВО:** Додайте ці змінні в Vercel Settings → Environment Variables:

```env
VITE_SUPABASE_URL=https://qcpuzfhawcondygspiok.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_cpQrhzVqZRCCeULDWhVJJw_ZIhcLx0Y
```

АБО (якщо Vercel не підтримує VITE_ префікс):

```env
NEXT_PUBLIC_SUPABASE_URL=https://qcpuzfhawcondygspiok.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_cpQrhzVqZRCCeULDWhVJJw_ZIhcLx0Y
```

**Як додати:**
1. Settings → Environment Variables
2. Додайте кожну змінну окремо
3. Виберіть середовища: Production, Preview, Development
4. Натисніть "Save"

### 3. Автоматичний деплой

✅ **Вже налаштовано:**
- Кожен push до `main` гілки автоматично створює новий деплой
- Pull Requests автоматично створюють Preview деплої

### 4. Перевірка деплою

Після першого деплою:
1. Перевірте, що build пройшов успішно
2. Відкрийте URL проекту (наприклад: `real-estate-platform.vercel.app`)
3. Перевірте, що сайт завантажується
4. Перевірте консоль браузера на помилки

## 📋 Чеклист перед деплоєм

- [x] ✅ `vercel.json` налаштовано
- [x] ✅ `package.json` має правильні скрипти
- [x] ✅ Environment variables готові
- [x] ✅ GitHub репозиторій підключено
- [ ] ⏳ Додати environment variables в Vercel
- [ ] ⏳ Запушити код на GitHub
- [ ] ⏳ Перевірити деплой

## 🔧 Налаштування `vercel.json`

Файл `vercel.json` вже налаштований:

```json
{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "headers": [
    {
      "source": "/assets/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=31536000, immutable"
        }
      ]
    }
  ]
}
```

**Що це робить:**
- `rewrites` - SPA routing (всі маршрути перенаправляються на `index.html`)
- `headers` - кешування статичних файлів для кращої продуктивності
- `framework: "vite"` - автоматичне визначення Vite проекту

## 🐛 Вирішення проблем

### Проблема: "Build failed"

**Рішення:**
1. Перевірте логи build в Vercel
2. Переконайтеся, що всі залежності в `package.json`
3. Перевірте, чи environment variables додані

### Проблема: "Environment variables not found"

**Рішення:**
1. Перевірте, що змінні додані в Vercel Settings
2. Переконайтеся, що використовується правильний префікс (`VITE_` або `NEXT_PUBLIC_`)
3. Перезапустіть деплой після додавання змінних

### Проблема: "404 on routes"

**Рішення:**
- Перевірте, що `rewrites` в `vercel.json` налаштовані правильно
- Переконайтеся, що використовується SPA routing

## 📝 Після успішного деплою

1. ✅ Перевірте, що сайт працює
2. ✅ Перевірте автентифікацію
3. ✅ Перевірте завантаження даних з Supabase
4. ✅ Налаштуйте custom domain (опціонально)

---

**Останнє оновлення:** 2025-01-XX

