# ⚡ Швидкий старт з GitHub Token

## Що робити з токеном від GitHub?

GitHub створив для вас **Personal Access Token** - це ваш "пароль" для push коду на GitHub.

## ⚠️ Спочатку встановіть Git

Якщо Git не встановлений, оберіть один з варіантів:

### Варіант A: Git for Windows (командний рядок)
- Завантажте: https://git-scm.com/download/win
- Встановіть з налаштуваннями за замовчуванням
- Перезапустіть PowerShell

### Варіант B: GitHub Desktop (простіше)
- Завантажте: https://desktop.github.com/
- Встановіть та увійдіть через GitHub
- Відкрийте папку проекту та натисніть "Publish repository"

**Детальна інструкція:** дивіться `GIT_SETUP.md`

## 🚀 Швидка інструкція (після встановлення Git)

### 1. Підготуйте код:
```powershell
# Перейдіть в директорію проекту
cd "G:\My Drive\!Hero rooms\v3 (1)"

# Перевірте статус
git status
```

### 2. Додайте файли та створіть commit:
```powershell
git add .
git commit -m "Initial commit: Real estate management platform"
```

### 3. Підключіть до GitHub (якщо ще не зроблено):
```powershell
# Замініть YOUR_USERNAME та YOUR_REPO_NAME на ваші дані
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
git branch -M main
```

### 4. Push на GitHub (тут знадобиться токен):
```powershell
git push -u origin main
```

**Коли Git попросить автентифікацію:**
- **Username**: введіть ваш GitHub username
- **Password**: вставте ваш токен (НЕ пароль!)

Токен зазвичай виглядає так: `ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

### 5. Windows збереже токен автоматично

Після першого успішного push, Windows збереже токен, і наступні рази не потрібно буде вводити його знову.

## ❓ Часті питання

**Q: Де знайти мій токен?**
A: Якщо ви вже створили токен, він показується тільки один раз. Якщо втратили - створіть новий на https://github.com/settings/tokens

**Q: Токен не працює?**
A: Перевірте, що токен має права `repo` (повний доступ до репозиторіїв)

**Q: Безпечно це?**
A: Так, токен зберігається локально в Windows Credential Manager і не передається нікому

## 📚 Детальна інструкція

Дивіться файл `GITHUB_TOKEN_GUIDE.md` для повної інформації.

## ✅ Після успішного push

1. Перейдіть на https://vercel.com
2. Підключіть ваш GitHub репозиторій
3. Vercel автоматично задеплоїть проект!

